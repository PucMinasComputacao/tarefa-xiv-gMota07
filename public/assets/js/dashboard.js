const API="http://localhost:3000/carros";fetch(API).then(r=>r.json()).then(cs=>{const m={},a={};cs.forEach(c=>{m[c.marca]=(m[c.marca]||0)+1;a[c.ano]=(a[c.ano]||0)+1;});new Chart(document.getElementById("marca"),{type:"pie",data:{labels:Object.keys(m),datasets:[{data:Object.values(m)}]}});new Chart(document.getElementById("ano"),{type:"bar",data:{labels:Object.keys(a),datasets:[{label:"Carros",data:Object.values(a)}]}});});